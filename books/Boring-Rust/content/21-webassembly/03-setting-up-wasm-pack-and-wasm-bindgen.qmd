---
title: "Setting Up: wasm-pack and wasm-bindgen"
---

# Setting Up: wasm-pack and wasm-bindgen
Install `wasm-pack`:

```bash
cargo install wasm-pack
```

Configure `Cargo.toml`:

```toml
[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
```
