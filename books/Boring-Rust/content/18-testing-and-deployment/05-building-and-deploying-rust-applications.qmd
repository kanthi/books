---
title: "Building and Deploying Rust Applications"
---

# Building and Deploying Rust Applications
Rust binaries can be statically compiled with musl libc for minimal, scratch Docker containers:

```bash
rustup target add x86_64-unknown-linux-musl
cargo build --release --target x86_64-unknown-linux-musl
```
