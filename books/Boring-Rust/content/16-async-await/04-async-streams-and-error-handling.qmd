---
title: "Async Streams and Error Handling"
---

# Async Streams and Error Handling
Streams are asynchronous iterators that yield values over time:

```rust
// Stream represents a stream of async items
// while let Some(item) = stream.next().await { ... }
```
