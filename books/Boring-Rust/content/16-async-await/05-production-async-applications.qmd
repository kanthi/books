---
title: "Building Production Async Applications"
---

# Building Production Async Applications
Best practices for async architectures:
- Never block async worker threads with long CPU-bound tasks (use `tokio::task::spawn_blocking`).
- Always implement timeouts on network requests (`tokio::time::timeout`).
- Implement graceful shutdown using cancellation tokens.
