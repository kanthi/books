# Archived front matter (not published)

Removed from the published Networking book spine on **2026-08-11**.

Syllabus variants and comparison pages live here for reference only.  
`content/` directories starting with `_` are ignored by `scripts/update-index.sh`.
