# Networking

Vendor-agnostic notes: **first principles → expert NetOps**, labs in **Containerlab** with **open/free images** only.

## Syllabi

See Front Matter: Syllabus (A) + Syllabus 1–7 + comparison index.

## Diagrams

`images/diagram-*.svg` — transparent SVG, `currentColor` for light/dark Quarto themes.

## Commands

```bash
bash scripts/update-index.sh
cd .. && ./indipub.sh Networking
```
