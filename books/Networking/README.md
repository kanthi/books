# Networking

Vendor-agnostic notes: **first principles → expert NetOps**, labs in **Containerlab** with **open/free images** only.

Curriculum map and journey are on `index.qmd` (parts 01–10). Planning notes (if any) live under `content/_planning/` (not published).

## Diagrams

Hand-written SVGs under `images/`. See **`images/README.md`** for the illustrated topology style (canvas, grid, IP chips, racks) and inventory.

| Kind | Style |
|------|--------|
| **Topology** | Illustrated light-card SVG (dist-switch, MAC, VLAN, triangle, leaf-spine) |
| **Conceptual** | Monochrome `#7a8fa6` on transparent (planes, encapsulation, verify loop, …) |

## Commands

```bash
bash scripts/update-index.sh
cd .. && ./indipub.sh Networking
```
