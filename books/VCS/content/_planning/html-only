Local `indipub.sh VCS` renders HTML only while this file exists.

Remove this file when core Git prose (Parts I–II, chapters 1–15) is filled and you want PDF/EPUB again. CI (`incremental.yml` / `renderpub.sh`) is unchanged and still builds all formats.
