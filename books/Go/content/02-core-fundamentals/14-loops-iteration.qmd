# Loops and Iteration

## Overview

Go has only one looping construct: the `for` loop. This single keyword handles all looping patterns.

## The Basic `for` Loop

```go
for i := 0; i < 10; i++ {
    fmt.Println(i)
}
```

## The `while` Loop (Condition Only)

```go
n := 1
for n < 100 {
    n *= 2
}
```

## The Infinite Loop

```go
for {
    if done {
        break
    }
}
```

## The `range` Loop

### Slices and Arrays

```go
nums := []int{10, 20, 30}

for i, v := range nums {
    fmt.Printf("%d: %d\n", i, v)
}

for _, v := range nums {  // Value only
    fmt.Println(v)
}
```

### Maps

```go
ages := map[string]int{"Alice": 30, "Bob": 25}

for key, value := range ages {
    fmt.Printf("%s: %d\n", key, value)
}
```

### Strings

```go
for i, r := range "Hello" {
    fmt.Printf("%d: %c\n", i, r)
}
```

### Channels

```go
for v := range ch {  // Exits when channel closes
    fmt.Println(v)
}
```

## Iterators (Go 1.23+)

Go 1.23 introduced "range-over-func," allowing you to use `range` with custom iterator functions.

### Sequence Iterators

A sequence iterator is a function that takes a yield function:
`func(yield func(V) bool)` (single value) or `func(yield func(K, V) bool)` (key-value).

```go
func All[T any](s []T) iter.Seq[T] {
    return func(yield func(T) bool) {
        for _, v := range s {
            if !yield(v) {
                return
            }
        }
    }
}

// Usage
for v := range All(nums) {
    fmt.Println(v)
}
```

### Pull Iterators

For more control, you can use pull iterators:

```go
next, stop := iter.Pull(All(nums))
defer stop()

for {
    v, ok := next()
    if !ok {
        break
    }
    fmt.Println(v)
}
```

## Loop Control

### `break` and `continue`

```go
for i := 0; i < 10; i++ {
    if i == 5 {
        break     // Exit loop
    }
    if i%2 == 0 {
        continue  // Skip to next iteration
    }
    fmt.Println(i)
}
```

### Labels for Nested Loops

```go
outer:
for i := 0; i < 3; i++ {
    for j := 0; j < 3; j++ {
        if i == 1 && j == 1 {
            break outer  // Break both loops
        }
    }
}
```

## Common Patterns

```go
// Reverse iteration
for i := len(items) - 1; i >= 0; i-- {
    process(items[i])
}

// Step by N
for i := 0; i < 100; i += 10 {
    fmt.Println(i)
}
```

## Summary

| Pattern | Syntax |
|---------|--------|
| Classic for | `for i := 0; i < n; i++ {}` |
| While loop | `for condition {}` |
| Infinite loop | `for {}` |
| Range | `for i, v := range slice {}` |
| Iterators | `for v := range myIter() {}` |

## Related Topics
- [Arrays and Slices](15-arrays-slices.qmd)
- [Maps](17-maps.qmd)
