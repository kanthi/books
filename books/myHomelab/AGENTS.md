# Agent Notes for `myHomelab`

- `_quarto.yml` is generated by `scripts/update-index.sh`; never hand-edit it.
- Chapter order is filename-driven:
  - part order: numeric prefix on directories under `content/`
  - chapter order: numeric prefix on files inside each part directory
- Keep chapter files as `NN-slug.qmd`.
- Put planning-only sources in `content/_planning/` so they are excluded from generated navigation.
