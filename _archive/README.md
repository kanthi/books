# Archive

Historical monorepo material kept for reference. **Not used by CI or day-to-day authoring.**

| Path | Contents |
|------|----------|
| `scripts/` | Older full-library render scripts superseded by `books/renderpub-codex-v2.sh` |
| `templates/Template.backup/` | Legacy book scaffold; prefer root `Template-New-1/` |
| `docs/` | Retired agent notes (e.g. short `Gemini.md`) |
| `book-backups/` | One-off `.bak` files found next to live books |
| `planning/` | Old syllabus option packs and pre-scratch planning (NixOS, Networking, …) |
| `content/` | Content appendices removed from live books but kept for reference |

Moved (not deleted). Active tooling and inventory: root `AGENTS.md`.
