# Legacy book template

This directory is the **previous** book scaffold, renamed from `Template/` for backup.

**Do not use for new books.** Prefer root **`Template-New-1/`**.

Kept only so older docs and habits can still inspect the prior layout (including the
old per-book GitHub workflow and `render_all_books.sh`).
